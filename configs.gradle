ext {
    version = [
            minSdkVersion    : 21,
            compileSdkVersion: 29,
            targetSdkVersion : 29,
            versionCode      : 10001,
            versionName      : "1.0.1",
    ]

    dependVersion = [
            appcompat       : "1.1.0",
    ]

    baseLib = [
            appcompat       : "androidx.appcompat:appcompat:$dependVersion.appcompat",
    ]

    baseLibs = baseLib.values()
}